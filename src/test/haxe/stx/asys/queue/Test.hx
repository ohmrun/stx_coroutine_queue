package stx.asys.queue;

using stx.Nano;
using stx.Test;
using stx.Log;

class Test extends TestCase{
  static public function main(){
    __.test().run(
      [
        new Test()
      ],[]
    );
  }
  public function test(){
    
  }
}